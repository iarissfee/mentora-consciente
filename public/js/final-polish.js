// Ajustes finales de UX: comunidad, PDFs, programas y ebooks.
(function(){
  const style=document.createElement('style');
  style.textContent=`
    .mc-preview-toolbar{display:flex;justify-content:flex-end;margin:0 0 18px}.mc-preview-toolbar button{border:1px solid #173a27;background:#fff;color:#173a27;padding:11px 17px;font-weight:700;cursor:pointer}.mc-community-preview-dialog{width:min(1180px,96vw);height:min(90vh,980px);max-width:none;border:0;padding:0;background:#f8f5f2;color:#172319}.mc-community-preview-dialog::backdrop{background:rgba(5,15,9,.76);backdrop-filter:blur(4px)}.mc-preview-shell{height:100%;overflow:auto;padding:28px}.mc-preview-top{position:sticky;top:0;z-index:3;display:flex;justify-content:space-between;align-items:center;background:#173a27;color:#fff;padding:13px 17px;margin:-28px -28px 24px}.mc-preview-top button{border:1px solid rgba(255,255,255,.6);background:transparent;color:#fff;padding:8px 13px;cursor:pointer}.mc-preview-head h2{font-family:"Bodoni Moda",Georgia,serif;font-size:42px;margin:4px 0}.mc-preview-head p{color:#666}.mc-preview-grid{display:grid;grid-template-columns:minmax(0,2fr) minmax(240px,.8fr);gap:22px}.mc-preview-post,.mc-preview-qr-card{background:#fff;border:1px solid #e1ddd8;padding:20px;margin-bottom:14px}.mc-preview-post strong{display:block}.mc-preview-post small{color:#777}.mc-preview-post img{width:100%;max-height:440px;object-fit:cover;margin-top:14px}.mc-preview-qr-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.mc-preview-qr-card img{width:100%;aspect-ratio:1;object-fit:contain}.mc-file-clear{margin-top:7px;border:1px solid #9d6b62;background:#fff;color:#8a3d32;padding:8px 11px;cursor:pointer}.mc-ebook-admin{margin-top:22px}.mc-ebook-editor{border-top:1px solid #ddd8d2;padding:22px 0}.mc-ebook-editor:first-of-type{border-top:0}.mc-ebook-pdf-state{background:#f4f1ec;padding:12px;margin:10px 0;font-size:12px}.mc-ebook-actions{display:flex;gap:10px;flex-wrap:wrap}.mc-owned-ebooks{margin-top:52px}.mc-owned-ebooks h2{font-family:"Bodoni Moda",Georgia,serif;font-size:38px;color:#153422}.mc-owned-ebook-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:18px}.mc-owned-ebook{background:#fff;border:1px solid #ddd8d2;padding:18px}.mc-owned-ebook img{width:100%;aspect-ratio:3/4;object-fit:cover}.mc-owned-ebook h3{font-family:"Bodoni Moda",Georgia,serif;font-size:25px;margin:14px 0 5px}.mc-owned-ebook a{display:block;margin-top:14px;background:#173a27;color:#fff;padding:11px;text-align:center;font-weight:700}.mc-admin-portal select[name="required_rank"],.mc-admin-portal input[name="allow_direct_purchase"]{display:none!important}
    @media(max-width:760px){.mc-preview-grid{grid-template-columns:1fr}.mc-preview-qr-grid,.mc-owned-ebook-grid{grid-template-columns:1fr 1fr}}@media(max-width:520px){.mc-preview-qr-grid,.mc-owned-ebook-grid{grid-template-columns:1fr}}
  `;
  document.head.appendChild(style);

  const baseLoad=loadAdminTab;
  loadAdminTab=async function(){
    await baseLoad();
    if(state.adminTab==='community')enhanceCommunityAdmin();
    if(state.adminTab==='courses')enhanceCourseAdmin();
    if(state.adminTab==='home')await appendEbookAdmin();
  };

  function enhanceCommunityAdmin(){
    const box=document.getElementById('admin-content');if(!box||box.querySelector('.mc-preview-toolbar'))return;
    const toolbar=document.createElement('div');toolbar.className='mc-preview-toolbar';toolbar.innerHTML='<button type="button" data-community-preview>👁 Vista como alumna</button>';
    box.prepend(toolbar);toolbar.querySelector('[data-community-preview]').onclick=openCommunityPreview
  }

  async function openCommunityPreview(){
    let posts=[],meds=[];try{posts=(await api('/api/community/posts')).posts||[]}catch{}try{meds=(await api('/api/community/meditations')).meditations||[]}catch{}
    const dlg=document.createElement('dialog');dlg.className='mc-community-preview-dialog';dlg.innerHTML=`<div class="mc-preview-shell"><div class="mc-preview-top"><strong>VISTA COMO ALUMNA · COMUNIDAD</strong><button type="button">Cerrar vista</button></div><header class="mc-preview-head"><p class="mc-kicker">EL SANTUARIO DE CONEXIÓN</p><h2>Comunidad</h2><p>Así ve la alumna las meditaciones y publicaciones.</p></header><div class="mc-preview-grid"><main><section><h3>Meditaciones</h3><div class="mc-preview-qr-grid">${meds.length?meds.map(m=>`<article class="mc-preview-qr-card"><img src="${e(m.qr_url)}" alt="QR ${e(m.title)}"><strong>${e(m.title)}</strong>${m.description?`<p>${e(m.description)}</p>`:''}</article>`).join(''):'<p>Todavía no hay meditaciones publicadas.</p>'}</div></section><section><h3>Publicaciones</h3>${posts.length?posts.map(p=>`<article class="mc-preview-post">${p.pinned?'<span class="tag">FIJADO</span>':''}<strong>${e(p.author_name)}</strong><small>${e(p.space||'General')} · ${fmtDate(p.created_at)}</small><p>${e(p.body)}</p>${p.image_url?`<img src="${e(p.image_url)}" alt="Publicación">`:''}<small>♥ ${p.likes_count||0} · ${p.comments_count||0} comentarios</small></article>`).join(''):'<p>Todavía no hay publicaciones.</p>'}</section></main><aside><div class="mc-preview-post"><strong>Espacios de foco</strong><p>Meditación Diaria</p><p>Desprogramación</p><p>Apoyo Grupal</p><p>Club de Lectura</p></div></aside></div></div>`;document.body.appendChild(dlg);dlg.querySelector('.mc-preview-top button').onclick=()=>dlg.close();dlg.addEventListener('close',()=>dlg.remove(),{once:true});dlg.showModal()
  }

  function enhanceCourseAdmin(){
    const box=document.getElementById('admin-content');if(!box)return;
    // Elimina controles heredados que ya no forman parte del modelo comercial.
    box.querySelectorAll('.mc-create-course').forEach(p=>{if(p.querySelector('input[name="allow_direct_purchase"],select[name="required_rank"]'))p.remove()});
    box.querySelectorAll('label').forEach(label=>{const txt=(label.textContent||'').trim().toLowerCase();if(txt==='nivel'||txt.startsWith('nivel requerido')||txt.includes('compra individual')||txt.includes('publicarlo ahora')||txt.includes('permitir compra individual')||txt.includes('quién puede descargarlo')){const input=label.querySelector('select[name="required_rank"],input[name="allow_direct_purchase"],input[name="published"]');if(input)label.remove()}});
    box.querySelectorAll('[data-delete-course]').forEach(b=>b.remove());
    box.querySelectorAll('.mc-course-footer-actions').forEach(footer=>{const label=footer.querySelector('.mc-customer-preview-label');if(label)label.innerHTML='<strong>👁 Vista como alumna</strong><span>Previsualizá exactamente cómo verá este programa una alumna con acceso.</span>';const links=[...footer.querySelectorAll('a[href^="#preview/"]')];links.forEach((a,i)=>{if(i===0){a.textContent='Abrir vista como alumna';const href=a.getAttribute('href')||'';a.setAttribute('href',href.replace(/\/\d+$/,'/0'))}else a.remove()})});
    box.querySelectorAll('.upload-pdf-form').forEach(addFileClear);
  }

  function addFileClear(form){const input=form.querySelector('input[type="file"][name="pdf"]');if(!input||form.querySelector('.mc-file-clear'))return;const btn=document.createElement('button');btn.type='button';btn.className='mc-file-clear';btn.textContent='Quitar archivo seleccionado';btn.hidden=!input.files?.length;input.insertAdjacentElement('afterend',btn);input.addEventListener('change',()=>btn.hidden=!input.files?.length);btn.onclick=()=>{input.value='';btn.hidden=true;toast('Archivo quitado. Podés elegir otro PDF.')}
  }

  async function appendEbookAdmin(){
    const box=document.getElementById('admin-content');if(!box||box.querySelector('.mc-ebook-admin'))return;
    let d;try{d=await api('/api/admin/ebooks')}catch{return}
    const section=document.createElement('section');section.className='panel mc-ebook-admin';section.innerHTML=`<div class="mc-panel-heading"><div><p class="mc-kicker">LIBRERÍA</p><h3>Ebooks de la Home</h3><p>Editá precio y portada, cargá el PDF real y eliminá o reemplazá el archivo cuando quieras.</p></div></div>${(d.ebooks||[]).map(x=>ebookEditor(x)).join('')}`;box.appendChild(section);bindEbookAdmin(section)
  }
  function ebookEditor(x){return `<div class="mc-ebook-editor" data-ebook="${x.id}"><form class="form-grid mc-ebook-data"><label>Título<input name="title" value="${e(x.title)}" required></label><label>URL<input name="slug" value="${e(x.slug)}" required></label><label>Precio<input name="price" type="number" min="0" step="1" value="${Number(x.price||0)}"></label><label>Moneda<select name="currency"><option ${x.currency==='USD'?'selected':''}>USD</option><option ${x.currency==='EUR'?'selected':''}>EUR</option></select></label><label>Orden<input name="sort_order" type="number" value="${Number(x.sort_order||0)}"></label><label><span><input name="active" type="checkbox" ${x.active?'checked':''}> Visible en Home</span></label><label style="grid-column:1/-1">Subtítulo<input name="subtitle" value="${e(x.subtitle||'')}"></label><label style="grid-column:1/-1">Portada (URL)<input name="cover_url" value="${e(x.cover_url||'')}"></label><button class="btn btn-primary" type="submit">Guardar ebook</button></form><div class="mc-ebook-pdf-state">${x.pdf_stored_name?`PDF cargado: <strong>${e(x.pdf_original_name||'ebook.pdf')}</strong> · ${(Number(x.pdf_bytes||0)/1024/1024).toFixed(2)} MB`:'Todavía no hay PDF cargado. Hasta que cargues uno, el botón Comprar no habilita una venta.'}</div><form class="mc-ebook-upload form-stack"><label>PDF del ebook<input type="file" name="pdf" accept="application/pdf,.pdf" required></label><div class="mc-ebook-actions"><button class="mini-btn mc-strong-btn" type="submit">${x.pdf_stored_name?'Reemplazar PDF':'Subir PDF'}</button>${x.pdf_stored_name?'<button type="button" class="mini-btn mc-delete" data-remove-ebook-pdf>Eliminar PDF</button>':''}</div></form></div>`}
  function bindEbookAdmin(section){section.querySelectorAll('[data-ebook]').forEach(card=>{const id=Number(card.dataset.ebook),dataForm=card.querySelector('.mc-ebook-data'),upload=card.querySelector('.mc-ebook-upload'),file=upload.querySelector('input[type="file"]');addFileClear(upload);dataForm.onsubmit=async ev=>{ev.preventDefault();const f=new FormData(dataForm);try{await api(`/api/admin/ebooks/${id}`,{method:'PUT',body:{title:f.get('title'),slug:f.get('slug'),price:Number(f.get('price')),currency:f.get('currency'),sort_order:Number(f.get('sort_order')),subtitle:f.get('subtitle'),cover_url:f.get('cover_url'),active:!!f.get('active')}});toast('Ebook actualizado');await loadAdminTab()}catch(x){toast(x.message)}};upload.onsubmit=async ev=>{ev.preventDefault();if(!file.files?.[0])return toast('Elegí un PDF.');const fd=new FormData();fd.append('pdf',file.files[0]);try{await api(`/api/admin/ebooks/${id}/pdf`,{method:'POST',body:fd});toast('PDF del ebook guardado');await loadAdminTab()}catch(x){toast(x.message)}};const del=card.querySelector('[data-remove-ebook-pdf]');if(del)del.onclick=async()=>{if(!confirm('¿Eliminar el PDF de este ebook? El producto dejará de estar disponible para nuevas compras hasta que subas otro.'))return;try{await api(`/api/admin/ebooks/${id}/pdf`,{method:'DELETE'});toast('PDF eliminado');await loadAdminTab()}catch(x){toast(x.message)}}})}

  // Agrega los ebooks comprados al campus de la alumna.
  const baseCampus=campusHtml;
  campusHtml=async function(){const html=await baseCampus();if(!state.me?.user||state.me.user.role==='admin')return html;let rows=[];try{rows=(await api('/api/my/ebooks')).ebooks||[]}catch{}if(!rows.length)return html;const block=`<section class="mc-owned-ebooks"><div class="mc-section-row"><h2>Mis ebooks</h2><span>${rows.length} comprados</span></div><div class="mc-owned-ebook-grid">${rows.map(x=>`<article class="mc-owned-ebook"><img src="${e(x.cover_url)}" alt="${e(x.title)}"><h3>${e(x.title)}</h3><p>${e(x.subtitle||'Ebook Digital')}</p><a href="/api/ebooks/${x.id}/download">Descargar PDF</a></article>`).join('')}</div></section>`;return html.replace('</main></section>',block+'</main></section>')};
})();
