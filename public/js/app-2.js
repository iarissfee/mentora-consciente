function homeHtml(){return `<section class="auth-gate"><p class="eyebrow">MENTORA CONSCIENTE</p><h2>Volvé a la página principal.</h2><p>Las opciones de compra están directamente en la Home.</p><a class="btn btn-primary" href="/">Ir al inicio</a></section>`}
function mentoriaOffer(c){const can=hasAccess(c);return `<article class="plan-card"><h3>${e(c.title)}</h3><p>${e(c.subtitle||c.description)}</p><div class="plan-price">${fmtMoney(c.price,c.currency)}</div>${can?`<a class="btn btn-primary" href="#course/${encodeURIComponent(c.slug)}">Entrar</a>`:`<a class="btn btn-primary" href="/">Comprar desde Inicio</a>`}</article>`}
function courseCard(c){const can=hasAccess(c);return `<article class="course-card"><div class="course-cover ${c.cover_url?'':'empty'}">${c.cover_url?`<img src="${e(c.cover_url)}" alt="${e(c.title)}">`:''}</div><div class="course-body"><h3>${e(c.title)}</h3><p>${e(c.subtitle||c.description)}</p><div class="card-meta"><span class="price">${fmtMoney(c.price,c.currency)}</span>${can?`<a class="btn btn-primary" href="#course/${encodeURIComponent(c.slug)}">Entrar</a>`:`<a class="btn btn-outline" href="/">Comprar</a>`}</div></div></article>`}
function planCard(p){return `<article class="plan-card"><h3>${e(p.name)}</h3><p>${e(p.description)}</p><div class="plan-price">${fmtMoney(p.price,p.currency)}</div><a class="btn btn-primary" href="/">Comprar desde Inicio</a></article>`}
function hasAccess(c){if(!state.me?.user)return false;if(state.me.user.role==='admin')return true;if((state.me.directCourseIds||[]).includes(c.id))return true;const rank=state.me.membership?.rank||0;return Number(c.required_rank)>0&&rank>=Number(c.required_rank)}
function bindShop(){$$('[data-buy]').forEach(b=>b.onclick=()=>{const type=b.dataset.buy,id=Number(b.dataset.id);const item=type==='course'?state.catalog.courses.find(x=>x.id===id):state.catalog.plans.find(x=>x.id===id);if(item)openCheckout(type,item)})}
function openCheckout(type,item){if(!state.me?.user){state.pendingCheckout={type,item};openAuth('register');return}state.pendingCheckout={type,item};$('#checkout-title').textContent=type==='plan'?item.name:item.title;$('#checkout-summary').innerHTML=`<div class="checkout-item"><span>${e(type==='plan'?item.name:item.title)}</span><strong>${fmtMoney(item.price,item.currency)}</strong></div><p class="secure-note">Cuando el pago quede confirmado, tu acceso se habilita automáticamente.</p>`;$('#checkout-message').textContent='';$('#paypal-pay-button').classList.toggle('hidden',!state.config.paypal.enabled);$('#demo-pay-button').classList.toggle('hidden',!state.config.demoCheckout);if(!state.config.paypal.enabled&&!state.config.demoCheckout)$('#checkout-message').textContent='Falta conectar la cuenta PayPal para habilitar pagos.';$('#checkout-dialog').showModal()}
async function loadPayPal(){if(state.paypalSdk)return state.paypalSdk;if(!state.config.paypal.enabled)throw new Error('PayPal todavía no está conectado.');if(!window.paypal){await new Promise((resolve,reject)=>{const s=document.createElement('script');s.async=true;s.src=`https://${state.config.paypal.mode==='live'?'www':'www.sandbox'}.paypal.com/web-sdk/v6/core`;s.onload=resolve;s.onerror=()=>reject(new Error('No se pudo cargar PayPal.'));document.head.appendChild(s)})}state.paypalSdk=await window.paypal.createInstance({clientId:state.config.paypal.clientId,components:['paypal-payments'],pageType:'checkout'});return state.paypalSdk}
async function payCurrent(demo){const p=state.pendingCheckout;if(!p)return;const type=p.type,id=p.item.id;const message=$('#checkout-message');message.textContent='';try{if(demo){const r=await api('/api/demo/purchase',{method:'POST',body:{itemType:type,itemId:id}});$('#checkout-dialog').close();await refreshMe();toast('Pago confirmado · acceso habilitado');location.href=r.redirect;return}const sdk=await loadPayPal();const btn=$('#paypal-pay-button');const session=sdk.createPayPalOneTimePaymentSession({onApprove:async data=>{message.textContent='Confirmando pago…';const cap=await api(`/api/paypal/capture-order/${encodeURIComponent(data.orderId)}`,{method:'POST',body:{}});await refreshMe();$('#checkout-dialog').close();toast('Pago confirmado · acceso habilitado');location.href=cap.redirect},onCancel:()=>{message.textContent='El pago fue cancelado. No se habilitó el acceso.'},onError:()=>{message.textContent='PayPal no pudo completar la operación. No se habilitó el acceso.'}});await session.start({presentationMode:'auto',targetElement:btn},api('/api/paypal/create-order',{method:'POST',body:{itemType:type,itemId:id}}).then(x=>({orderId:x.id}))) }catch(x){message.textContent=x.message}}
