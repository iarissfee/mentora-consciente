  const defaults={brand_name:'MENTORA CONSCIENTE',site_title:'Campus · Cambia tu realidad',hero_eyebrow:'FORMACIÓN ONLINE · CAMPUS PRIVADO',hero_title:'Convertí tu experiencia en una marca que trabaja para vos.',hero_text:'Cursos, recursos y acompañamiento para construir una presencia profesional, monetizable y propia.',support_email:'hola@tumarca.com'};
  const ins=db.prepare('INSERT OR IGNORE INTO settings(key,value) VALUES(?,?)');for(const [k,v] of Object.entries(defaults))ins.run(k,v);
  const adminEmail=safeEmail(process.env.ADMIN_EMAIL||'admin@example.com');if(!db.prepare('SELECT id FROM users WHERE email=?').get(adminEmail)){const pass=process.env.ADMIN_PASSWORD||'CHANGE-ME-NOW-123!';db.prepare('INSERT INTO users(name,email,password_hash,role,created_at) VALUES(?,?,?,?,?)').run('Administración',adminEmail,hashPassword(pass),'admin',nowIso());if(!isProd)console.log(`[demo] Admin: ${adminEmail} / ${pass}`)}
}
initDb();

const rates=new Map();
function rateOk(key,limit,windowMs){const now=Date.now();let r=rates.get(key);if(!r||now-r.start>windowMs)r={start:now,count:0};r.count++;rates.set(key,r);return r.count<=limit}
function clientIp(req){return (req.headers['x-forwarded-for']||req.socket.remoteAddress||'').toString().split(',')[0].trim()}
function parseCookies(req){const out={};for(const p of String(req.headers.cookie||'').split(';')){const i=p.indexOf('=');if(i<0)continue;out[p.slice(0,i).trim()]=decodeURIComponent(p.slice(i+1).trim())}return out}
function setCookie(res,name,value,opts={}){let s=`${name}=${encodeURIComponent(value)}`;s+=`; Path=${opts.path||'/'}`;if(opts.maxAge)s+=`; Max-Age=${Math.floor(opts.maxAge/1000)}`;if(opts.httpOnly)s+='; HttpOnly';if(opts.secure)s+='; Secure';if(opts.sameSite)s+=`; SameSite=${opts.sameSite}`;res.setHeader('Set-Cookie',[...(res.getHeader('Set-Cookie')||[]),s])}
function clearCookie(res,name){setCookie(res,name,'',{maxAge:1,httpOnly:true,sameSite:'Lax',secure:isProd})}
function securityHeaders(res){res.setHeader('X-Content-Type-Options','nosniff');res.setHeader('X-Frame-Options','DENY');res.setHeader('Referrer-Policy','strict-origin-when-cross-origin');res.setHeader('Permissions-Policy','camera=(), microphone=(), geolocation=()');res.setHeader('Cross-Origin-Opener-Policy','same-origin-allow-popups');res.setHeader('Content-Security-Policy',"default-src 'self'; script-src 'self' https://www.paypal.com https://www.sandbox.paypal.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com data:; img-src 'self' data: https:; connect-src 'self' https://api-m.paypal.com https://api-m.sandbox.paypal.com https://www.paypal.com https://www.sandbox.paypal.com; frame-src https://www.youtube.com https://www.youtube-nocookie.com https://player.vimeo.com https://www.paypal.com https://www.sandbox.paypal.com; object-src 'none'; base-uri 'self'; form-action 'self' https://www.paypal.com https://www.sandbox.paypal.com");if(isProd)res.setHeader('Strict-Transport-Security','max-age=31536000; includeSubDomains')}
